import io, os
os.chdir(r"C:\GREENCORE\AIO\gsc-marketplace")
def rd(p): return io.open(p, encoding="utf-8").read()
def wr(p, s): io.open(p, "w", encoding="utf-8", newline="\n").write(s)
def rep(s, a, b, p):
    assert a in s, (p, a[:70]); return s.replace(a, b, 1)

# ---------- kit.js ----------
k = rd("kit.js")
k = rep(k, "Every answer carries both doors: Instant Messaging for agents, the GSC Trading Desk for humans. Free — no payment machinery.`],",
       "Every answer carries both doors: Instant Messaging for agents, the GSC Trading Desk for humans. Open endpoint — no authentication, no payment.`],", "kit")
old_x = k[k.index('  "x402-capability": ['):k.index("};\nconst skillMd")]
new_skills = r'''  "x402-capability": ["Read the GSC-Marketplace x402 capability declaration — header, network, asset, status — and where the single paid endpoint lives.",
    `Header 19 on every gen-2 GSC surface: x-gsc-x402: ready. Network ${POSTURE.x402.network} (${POSTURE.x402.chain}), asset ${POSTURE.x402.asset}, status ${POSTURE.x402.status}.\nThe single paid endpoint is https://${HOSTS.apex}/x402/resolve — its terms are stated in its 402 payload only, never in copy. Every other endpoint on this surface is open: no authentication, no payment.\nDoor (machine-doc): https://${HOSTS.apex}/x402 · JSON: https://${HOSTS.apex}/x402.json · to settle, read the x402-settlement skill.`],
  "x402-settlement": ["Settle against the GSC-Marketplace x402 test endpoint unassisted — the 402, what to sign (EIP-3009 USDC on Base), the retry with PAYMENT-SIGNATURE, the signed receipt and how to verify it offline.",
    `## 1 · Ask\nGET https://${HOSTS.apex}/x402/resolve with no payment header → HTTP 402. The header PAYMENT-REQUIRED carries base64 of the JSON body; the body is the x402 v2 shape:\n{ x402Version: 2, error, resource: { url, description, mimeType }, accepts: [ { scheme: "exact", network: "${POSTURE.x402.chain}", amount, asset, payTo, maxTimeoutSeconds: 60, extra: { assetTransferMethod: "eip3009", name: "USD Coin", version: "2" } } ] }\nThe amount and the receive address (payTo) are stated in that payload only. Read them from the payload every time — never from copy.\n\n## 2 · Sign\nasset is USDC on Base mainnet (${POSTURE.x402.chain}). Sign an EIP-3009 transferWithAuthorization (EIP-712, domain name "USD Coin" version "2", chainId 8453, verifyingContract = asset) with from = your wallet, to = payTo, value = amount, validAfter = 0, validBefore = now + maxTimeoutSeconds, nonce = 32 random bytes. The transfer is gasless for the payer — the facilitator submits it.\nBuild the PaymentPayload: { x402Version: 2, accepted: the accepts[0] object you are paying (copied verbatim from the 402), payload: { signature, authorization: { from, to, value, validAfter, validBefore, nonce } } }.\n\n## 3 · Retry\nGET https://${HOSTS.apex}/x402/resolve with header PAYMENT-SIGNATURE: base64(PaymentPayload JSON). The server verifies and settles through its facilitator.\n- 402 again → the payload was rejected; the JSON error names the reason (for example invalid_exact_evm_payload_signature). Nothing moved.\n- 502 → settlement failed after a valid verify; retry with a fresh nonce.\n- 200 → settled. Header PAYMENT-RESPONSE carries base64 of { success: true, transaction, network, payer }.\n\n## 4 · The receipt\nThe 200 body is the signed receipt: confirmation, timestamp, amount, asset, asset_symbol, network, payer, tx_hash, nonce, receipt_url, issuer, duns, signature { alg: "EdDSA", kid: "gsc-x402-receipts-2026-08", canonicalization, value }, ia_message_ticket, next_steps.\nIt is durable: GET https://${HOSTS.apex}/x402/receipt/ followed by the nonce (24 hex characters) returns it forever, as JSON or HTML by Accept. The nonce is the claim ticket.\n\n## 5 · Verify offline\nRemove signature, ia_message_ticket and next_steps. Serialize the rest as JSON with keys sorted recursively, no whitespace, UTF-8 (non-ASCII unescaped). Verify signature.value (base64url, Ed25519) over those bytes against the key with kid gsc-x402-receipts-2026-08 at https://${HOSTS.apex}/x402/jwks.json. Check tx_hash on Base if you want the chain's word as well.\n\n## 6 · After\nEvery settlement also emits an IA-MESSAGE (subject "x402-settlement · " + nonce) to the transaction MCP ${DOORS.instant_messaging.mcp}, where it appears as a read-only desk row. The open doors after settlement: Instant Messaging for agents (${DOORS.instant_messaging.inbound}), the GSC Trading Desk for humans (${TRADING_DESK}), brand sourcing on https://${HOSTS.mcp}/mcp.\nCompleting this flow from this file alone is the conformance test — no GSC help, no account, no human.`],
  "aio-agent-fleet": ["Resolve and verify the 10,000 AI Orderability (AIO) Agents — regional hosts by name, slot addressing, signed Cards checked against each host's JWKS, the fleet tool on the MCP.",
    `## The class\n${AIO_FLEET.class}. ${AIO_FLEET.program_total.toLocaleString("en-US")} Agents, ${AIO_FLEET.per_host.toLocaleString("en-US")} per host, unit of remit = ${AIO_FLEET.unit_of_remit}.\n\n## Hosts by name\n${AIO_FLEET.hosts.map((h) => `${h.cc} → https://${h.host} (${h.jurisdiction}, ${h.region.split(" (")[0]})`).join("\n")}\n\n## Addressing\nSlot = four digits 0001–1000 on a host. Card: https://HOST/SLOT/.well-known/agent-card.json where HOST is one of the ten hosts above and SLOT the four digits (the bare /SLOT path also answers the Card); orderability resolution: /SLOT/orderability; unknown slots answer 404.\nHost index: /index.json (count, slot_range, key_id, allocation by category, agents[]); manifest: /manifest.json (cards, index_sha256 — the SHA-256 of the raw index bytes, allocation); keys: /.well-known/jwks.json.\n\n## Verify a Card\n1. GET the Card. 2. Remove its signature object. 3. Serialize the rest as JSON with keys sorted recursively, no whitespace, UTF-8 (non-ASCII unescaped). 4. Verify signature.value (base64url, Ed25519) over those bytes against the key with kid gsc-aio-cards-2026-08 in that host's /.well-known/jwks.json. 5. Optionally fetch /index.json raw and confirm its SHA-256 equals manifest.index_sha256.\nA Card that verifies is an AIO Agent; one that does not is not, whatever it claims.\n\n## Fleet numbers\nMCP tool get_aio_agent_fleet on https://${HOSTS.mcp}/mcp returns the class, the hosts and the program totals. Gen-1 Carrier numbers are read from ${AIO_FLEET.gen1_reference.live_truth}, never from here.\nEvery host answers with the 19 GSC headers (the Nineteen) and is agent-readiness scanned.`],
  "cpg-68000-signals": ["The seven CPG-68000 signals for agents — each enumerated with its state, its ACM-68000 definition, and where the machine register lives.",
    `CPG-68000 is the CPG application profile of ACM-68000 (open, MIT). Same seven signals, same numbers, same locked meanings: a CPG signal resolves to the ACM signal with the same number. The wire emits ACM- codes (x-gsc-signal) — an agent reading x-gsc-signal: ACM-300 on any GSC surface is reading CPG-300.\n\n| Signal | State | Action | Resolves to |\n|---|---|---|---|\n| CPG-000 | NOT_APPLICABLE | No action required. | ACM-000 |\n| CPG-200 | ALLOW | Execute the transaction. | ACM-200 |\n| CPG-300 | CONDITIONAL | Evaluate before proceeding. | ACM-300 |\n| CPG-403 | RESTRICT | Halt transaction. | ACM-403 |\n| CPG-404 | NOT_FOUND | Abort transaction. | ACM-404 |\n| CPG-451 | ESCALATE | Route to a human. | ACM-451 |\n| CPG-500 | SYSTEM_ERROR | Retry or escalate. | ACM-500 |\n\nSeven. Append-only. Non-breaking. Non-substitutable.\n\nMachine register: https://cpg-68000.ai/signals.json (each signal with its GSC-registered GTIN; parent GTIN 990832300716) · https://cpg-68000.ai/protocol.json · canon resolves live on mcp.cpgagentprotocols.ai (list_signals, resolve_signal) and on acm-68000.ai / acm-68000.org.\nOn this surface: every response carries x-gsc-signal and x-gsc-state (headers of the Nineteen); the MCP answers carry signal + state in the envelope; get_acm_68000 on https://${HOSTS.mcp}/mcp returns the protocol block.\nSignal doors: cpg-000 · cpg-200 · cpg-300 · cpg-403 · cpg-404 · cpg-451 · cpg-500 on .ai / .com / .org / .io — one signal per door, mirroring the acm- twins.`],
'''
k = k.replace(old_x, new_skills, 1)
k = rep(k, "Everything on this surface is **open and free by design** — no authentication, no payment, no 402 challenge.",
       "Everything on this surface is **open by design** — no authentication, no payment — with one exception: /x402/resolve, the single paid endpoint, whose terms are stated in its 402 payload only.", "kit auth.md")
k = rep(k, "Search Beauty & Personal Care brands in one market on the CPG Knowledge Graph (live, free).",
       "Search Beauty & Personal Care brands in one market on the CPG Knowledge Graph (live).", "kit webmcp")
old_card = k[k.index('      { id: "x402-capability", name: "x402 capability"'):k.index('    protocol: "ACM-68000", registry: REGISTRY, mcp_endpoint: mcpUrl }); });')]
new_card = r'''      { id: "x402-capability", name: "x402 capability", description: `x-gsc-x402: ready · ${POSTURE.x402.network} ${POSTURE.x402.chain} ${POSTURE.x402.asset} · ${POSTURE.x402.status} · single paid endpoint https://${HOSTS.apex}/x402/resolve, terms in its 402 payload only.`, tags: ["x402","capability"] },
      { id: "x402-settlement", name: "x402 settlement", description: `Settle against https://${HOSTS.apex}/x402/resolve unassisted: v2 402 → EIP-3009 USDC on Base → PAYMENT-SIGNATURE → signed receipt, verifiable offline against /x402/jwks.json, durable at /x402/receipt/ plus nonce. Skill: /.well-known/agent-skills/x402-settlement/SKILL.md`, tags: ["x402","payments","base","usdc","conformance"] },
      { id: "aio-agent-fleet", name: "AIO Agent fleet", description: `Resolve and verify the ${AIO_FLEET.program_total.toLocaleString("en-US")} AIO Agents: ${AIO_FLEET.hosts.map((x) => x.cc).join(" ")} hosts, slot Cards signed Ed25519 (kid gsc-aio-cards-2026-08) against each host's JWKS; get_aio_agent_fleet on the MCP. Skill: /.well-known/agent-skills/aio-agent-fleet/SKILL.md`, tags: ["aio","fleet","identity","ed25519"] },
      { id: "cpg-68000-signals", name: "CPG-68000 signals", description: "The seven CPG-68000 signals (000 200 300 403 404 451 500), each resolving to its ACM-68000 definition; register at https://cpg-68000.ai/signals.json. Skill: /.well-known/agent-skills/cpg-68000-signals/SKILL.md", tags: ["cpg-68000","acm-68000","signals"] }],
'''
k = k.replace(old_card, new_card, 1)
k = rep(k, 'import { OPERATOR, DUNS, KG, DOORS, HOSTS, TOOL_NAMES, VERSION, POSTURE } from "./canon.js";',
       'import { OPERATOR, DUNS, KG, DOORS, HOSTS, TOOL_NAMES, VERSION, POSTURE, AIO_FLEET } from "./canon.js";', "kit import")
wr("kit.js", k)

# ---------- server.js ----------
s = rd("server.js")
s = rep(s, "// in every complete answer. Free: no payment machinery, no 402 challenge.", "// in every complete answer. Open endpoints; the single paid endpoint is /x402/resolve (x402.js).", "srv")
s = rep(s, 'The fifth GSC surface: selection. Free.";', 'The fifth GSC surface: selection.";', "srv DESC")
s = rep(s, 'posture: "free — no payment machinery, no 402 challenge; x402 declared as capability (x-gsc-x402: ready)"',
       'posture: "open — no authentication, no payment on this MCP; x402 live: the single paid endpoint is https://gsc-marketplace.ai/x402/resolve, terms in its 402 payload only (x-gsc-x402: ready)"', "srv posture")
s = rep(s, "in one of 50 markets (live, free).", "in one of 50 markets (live).", "srv 98")
s = rep(s, 'description: "What GSC-Marketplace declares about x402: network, asset, status — and nothing else. Capability, never terms: no price is published on any GSC surface, no 402 challenge is issued; everything here is free in v1.",',
       'description: "What GSC-Marketplace declares about x402: header, network, asset, status — and where the single paid endpoint lives (https://gsc-marketplace.ai/x402/resolve; its terms are stated in its 402 payload only, never here). Door: https://gsc-marketplace.ai/x402 · skill: x402-settlement.",', "srv x402 tool")
s = rep(s, '}, async () => t(envelope({ signal: "ACM-200", state: "ALLOW", posture: POSTURE.v1, x402: POSTURE.x402, wire: "every gen-2 GSC response carries x-gsc-x402: ready (header 19 of the Ghost Nineteen)" })));',
       '}, async () => t(envelope({ signal: "ACM-200", state: "ALLOW", posture: POSTURE.v1, x402: POSTURE.x402, door: "https://gsc-marketplace.ai/x402", paid_endpoint: "https://gsc-marketplace.ai/x402/resolve", skill: "https://gsc-marketplace.ai/.well-known/agent-skills/x402-settlement/SKILL.md", wire: "every gen-2 GSC response carries x-gsc-x402: ready (header 19 of the Ghost Nineteen)" })));', "srv x402 out")
s = rep(s, "Everything free; x402 declared as capability (x-gsc-x402: ready).",
       "x402 live: the single paid endpoint is https://gsc-marketplace.ai/x402/resolve (terms in its 402 payload only); every other endpoint is open (x-gsc-x402: ready).", "srv what")
wr("server.js", s)

# ---------- canon.js ----------
c = rd("canon.js")
c = rep(c, 'v1: "Max discovery, everything FREE. No payment machinery, no HTTP 402 challenges. x402 is displayed as capability only.",',
       'v1: "Max discovery. Every endpoint open — no authentication, no payment — except one: /x402/resolve, the live x402 test endpoint, whose terms are stated in its 402 payload only. No pricing is published in copy on any GSC surface.",', "canon v1")
c = rep(c, '    status: "staged",', '    status: "live",', "canon status")
c = c.replace('"1.1.6"', '"1.2.0"', 1); wr("canon.js", c)

# ---------- x402.js ----------
x = rd("x402.js")
x = rep(x, "  const canon = JSON.stringify(body, Object.keys(body).sort());",
       '  const sortDeep = (v) => Array.isArray(v) ? v.map(sortDeep) : (v && typeof v === "object") ? Object.fromEntries(Object.keys(v).sort().map((k) => [k, sortDeep(v[k])])) : v;\n  const canon = JSON.stringify(sortDeep(body));', "x402 canon")
x = rep(x, "<h2>Free doors</h2>", "<h2>Open doors</h2>", "x402 doors")
x = x.replace("// /x402            FREE door (machine-doc register)", "// /x402            open door (machine-doc register)", 1)
wr("x402.js", x)
wr("package.json", rd("package.json").replace('"1.1.6"', '"1.2.0"', 1))
print("patched")
